paho-mqtt>=1.6,<2.0
PyYAML>=5.3
requests>=2.25      # fan_controller.py Huawei iBMC Redfish backend only
# HPE iLO4 backend uses the system ssh client + sshpass (apt install sshpass
# openssh-client) for fan control — no extra Python packages needed.
